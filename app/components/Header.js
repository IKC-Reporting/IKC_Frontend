import React from 'react';

const Header = () => (
    <header style={headerStyle}>
        IKC Tracker
    </header>
);

const headerStyle = {
    backgroundColor: '#f8f8f8',
    padding: '20px',
    textAlign: 'center',
    fontSize: '24px',
    fontWeight: 'bold'
};

export default Header;
